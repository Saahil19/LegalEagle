# LegalEagle Backend — Production Dependencies (Render Free Tier)
# Uses CPU-only torch from PyTorch CDN (~200MB vs ~800MB for CUDA build)

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web Framework ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# --- Database ---
SQLAlchemy==2.0.50

# --- ML Core (CPU-only torch — no GPU on Render) ---
torch==2.1.2+cpu
transformers==4.40.0
tokenizers==0.19.1
safetensors==0.4.3
accelerate==0.29.3
sentencepiece==0.2.0

# --- Embeddings & Vector Store ---
sentence-transformers==2.7.0
qdrant-client==1.9.1

# --- LangChain ---
langchain==0.2.1
langchain-core==0.2.3
langchain-huggingface==0.0.3
langchain-qdrant==0.1.1
langchain-text-splitters==0.2.1

# --- Celery (imported at module level, still needed) ---
celery==5.3.6
redis==5.0.4

# --- Utilities ---
pydantic==2.7.1
numpy==1.26.4
pypdf==4.2.0
python-dotenv==1.0.1
requests==2.31.0
tqdm==4.66.4
