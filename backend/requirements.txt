# LegalEagle Backend — Production Dependencies (Render Free Tier)
# ML packages (torch, transformers) intentionally excluded:
#   - BERT model files are gitignored and not on Render
#   - App uses keyword-based NER fallback + RISK_BASELINE heuristics
#   - Full ML pipeline runs locally where model files exist

# --- Web Framework ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
starlette==1.3.0

# --- Database ---
SQLAlchemy==2.0.50

# --- Celery (imported at module level, required even without Redis) ---
celery==5.3.6
redis==5.0.4
kombu==5.3.4
billiard==4.2.1
vine==5.1.0

# --- Utilities ---
pydantic==2.7.1
numpy==1.26.4
pypdf==4.2.0
python-dotenv==1.0.1
requests==2.31.0
tqdm==4.66.4
