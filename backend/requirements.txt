# LegalEagle Backend — Production Dependencies
# Note: torch is pinned to CPU-only build for Render free tier (no GPU available)

# --- Web Framework ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
starlette==1.3.0

# --- Database ---
SQLAlchemy==2.0.50

# --- ML Core (CPU torch for Render — no CUDA available) ---
torch==2.13.0
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
accelerate==1.13.0
sentencepiece==0.2.1

# --- Embeddings & Vector Store ---
sentence-transformers==5.5.1
qdrant-client==1.18.0

# --- LangChain ---
langchain==1.3.4
langchain-core==1.4.1
langchain-huggingface==1.2.2
langchain-qdrant==1.1.0
langchain-text-splitters==1.1.2

# --- Celery (imported at module level, still needed even without Redis) ---
celery==5.6.3
redis==8.0.0

# --- Utilities ---
pydantic==2.13.4
numpy==2.4.4
pypdf==6.13.0
python-dotenv==1.2.2
requests==2.34.2
tqdm==4.68.1
